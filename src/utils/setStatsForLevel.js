const setStatsForLevel = (mercenary) => {
    //TODO: Develop formula for updating stats based on BST and level
    //TODO: Take in mercenary and apply current stats
    console.log(mercenary);
};

export default setStatsForLevel;
